spring:
  application:
    name: compute-service
  cloud:
    consul:
      host: localhost
      port: 8500
server:
  port: 2222
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1112/eureka/

